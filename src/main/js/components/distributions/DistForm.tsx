import React from "react";
import Form from "../Form";
import { Binomial, Exponential, Geometric, Normal, Poisson, T } from '../util/DistTypes';

const DistForm = () => {
    return (
        <div>
            <Form />
        </div>
    )
}

export default DistForm;